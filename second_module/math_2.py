x = 3.14
y = -4
z = 5

#round() is a function in python that rounds the float to an int
print(round(x))

#abs() is a function that determines the absolute value of a variable
print(abs(y))

#pow(x, n) is a function to perform the power of an x to an n:
print(pow(z, 3))

#max() is a function that determines the maximum element between multiple variables:
print(max(x, y, z))

#min() is a function that determines the minimum element between multiple variables:
print(min(x, y, z))